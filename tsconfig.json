{
  "//": "Only for tools run from the repository root (shadcn add): `@/` is the site in presentation/. The site builds with presentation/tsconfig.json.",
  "compilerOptions": { "baseUrl": ".", "paths": { "@/*": ["./presentation/*"] } },
  "include": []
}
